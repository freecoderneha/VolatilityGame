<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hello.abc.neha.volatilitygame">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdvisorLogin"></activity>
        <activity android:name=".InvestorLogin"></activity>
        <activity android:name=".AdvisorIndian"></activity>
        <activity android:name=".ViewTutorial"></activity>
        <activity android:name=".PlayGame"></activity>
        <activity android:name=".UpdateProfile"></activity>
        <activity android:name=".InvestorIndian"></activity>
        <activity android:name=".MainActivity1"></activity>
    </application>

</manifest>